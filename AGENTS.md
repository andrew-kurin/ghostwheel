# Ghostwheel Agent Notes

Ghostwheel intentionally does not try to limit the `bash` tool, add command safety gates, or enforce filesystem restrictions for shell commands. Run the agent only in a sandboxed environment appropriate for the worktree and data you are willing to expose.
